extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/add", method="post")
    label(for="coverImg")
      input.form-control(type="file" name="coverImg" enctype="multipart/form-data")

